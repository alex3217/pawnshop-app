# Runtime
APP_NAME=pawnloop-api
NODE_ENV=development
APP_ENV=development
PORT=6002
PAWN_PORT=6002
HOST=0.0.0.0
DOTENV_CONFIG_PATH=.env.development
# Set to 1 only when the selected environment file must override inherited process values.
PAWN_ENV_OVERRIDE=0

# Networking / proxy
TRUST_PROXY=0

# Public authentication abuse controls (process-local; keep enabled in production)
AUTH_RATE_LIMIT_ENABLED=true
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_IP_MAX=30
AUTH_RATE_LIMIT_SENSITIVE_IP_MAX=10
AUTH_RATE_LIMIT_IDENTIFIER_MAX=20
AUTH_RATE_LIMIT_COMBINED_MAX=5

# URLs / CORS
FRONTEND_URL=https://pawnloop.com
WEB_URL=https://pawnloop.com
CORS_ORIGIN=https://pawnloop.com
CORS_ORIGINS=https://pawnloop.com,https://www.pawnloop.com

# Product media storage
# Local development writes validated images to this directory.
# Production remains blocked until a production-safe provider is implemented.
PRODUCT_MEDIA_STORAGE_PROVIDER=local-development
PRODUCT_MEDIA_LOCAL_DIRECTORY=uploads/product-media

# Request limits
JSON_LIMIT=2mb
URLENCODED_LIMIT=2mb

# Database
DATABASE_URL=postgresql://example-user:test-only-password@localhost:5432/pawnshop_dev?schema=public
# Required in staging and production deployments. Set both from deployment-managed
# configuration to the exact hostname and database name present in DATABASE_URL.
# These values identify the target; they are not credentials or connection URLs.
EXPECTED_DATABASE_HOST=replace-with-exact-database-host.invalid
EXPECTED_DATABASE_NAME=replace-with-exact-database-name

# Health / readiness
READINESS_TIMEOUT_MS=5000

# Auth
JWT_SECRET=replace-with-jwt-signing-secret
ACCESS_TOKEN_SECRET=replace-with-secret
JWT_ACCESS_SECRET=replace-with-jwt-secret
AUTH_SECRET=replace-with-secret
INTEGRATION_CREDENTIAL_ENCRYPTION_KEY=replace-with-credential-encryption-key
JWT_EXPIRES_IN=7d
EMAIL_VERIFICATION_TOKEN_TTL_MINUTES=1440
PASSWORD_RESET_TOKEN_TTL_MINUTES=60
# Explicit public-registration gate. Set true only after issuing beta invites.
INVITE_ONLY_REGISTRATION_ENABLED=false
EMAIL_PROVIDER=resend
EMAIL_FROM=PawnLoop <no-reply@notifications.pawnloop.com>
RESEND_API_KEY=
RESEND_API_TIMEOUT_MS=10000
# Set EMAIL_PROVIDER=smtp only for environments that intentionally retain SMTP.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=replace-with-smtp-user
SMTP_PASS=replace-with-smtp-password
# Fail promptly when an SMTP provider cannot be reached (milliseconds).
SMTP_CONNECTION_TIMEOUT_MS=8000
SMTP_GREETING_TIMEOUT_MS=8000
SMTP_SOCKET_TIMEOUT_MS=10000

# Stripe
STRIPE_SECRET_KEY=replace-with-api-key
STRIPE_WEBHOOK_SECRET=replace-with-webhook-secret
# Signing secret for connected-account events sent to /api/webhooks/stripe/connect.
STRIPE_CONNECT_WEBHOOK_SECRET=replace-with-webhook-secret
STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
STRIPE_DEFAULT_CURRENCY=usd
STRIPE_WEBHOOK_BODY_LIMIT=2mb

# Stripe seller subscription price IDs
STRIPE_PRICE_PRO=price_replace_me
STRIPE_PRICE_PREMIUM=price_replace_me
STRIPE_PRICE_ULTRA=price_replace_me
# Stripe buyer subscription price IDs
STRIPE_PRICE_BUYER_PLUS=price_replace_me
STRIPE_PRICE_BUYER_PREMIUM=price_replace_me
STRIPE_PRICE_BUYER_ULTRA=price_replace_me
STRIPE_CONNECT_ENABLED=false
SELLER_PAYOUT_MINIMUM_CENTS=1000

# Jobs / schedulers
AUCTION_SCHEDULER_ENABLED=false

# OpenAI / AI helpers
OPENAI_API_KEY=replace-with-api-key
OPENAI_LISTING_MODEL=gpt-4o-mini
AI_LISTING_ASSISTANT_ENABLED=true


AUCTION_SCHEDULER_INTERVAL_MS=5000
AUCTION_SCHEDULER_BATCH_SIZE=50
MARKETPLACE_RESERVATION_SCHEDULER_ENABLED=false
MARKETPLACE_RESERVATION_SCHEDULER_INTERVAL_MS=60000
MARKETPLACE_RESERVATION_SCHEDULER_BATCH_SIZE=50
MARKETPLACE_RESERVATION_TTL_MINUTES=30

# App metadata
APP_VERSION=development

# Combined frontend + API hosting
FRONTEND_HOSTS=pawnloop.com,www.pawnloop.com
SERVE_WEB=0
